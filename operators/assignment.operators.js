// assignment operators
// ? =
// ? +=
// ? -=
// ? *=
// ? /=
// ? %=
// ? **=

let x = 7;
let y = 3;

//?  =
// let sum = x + y;

// ? +=
// x += y; //=> x=x+y

// ? -=
// x -= y; //  x=x-y

// ? *=
// x *= y; // x= x*y

// ? /=
// x /= y; // x=x/y

//? %=
// x %= y; // x= x%y

// ? **=
// x **= y; // x=x ** y
// x **= 2; // x = x ** 2
// console.log(x);
